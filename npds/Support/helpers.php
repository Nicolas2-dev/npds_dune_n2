<?php

if (! function_exists('with'))
{
    /**
     * Renvoie l'objet donné. Utile pour enchaîner.
     *
     * @param  mixed  $object
     * @return mixed
     */
    function with($object)
    {
        return $object;
    }
}
